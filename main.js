// Language translations
const translations = {
    en: {
        home: "Home",
        services: "Services",
        projects: "Projects",
        about: "About",
        contact: "Contact",
        faq: "FAQ",
        heroTitle: "We Create <span class='accent-text'>Digital</span> Experiences",
        heroDesc: "Transforming ideas into cutting-edge web and mobile solutions that elevate your brand.",
        ourServices: "Our Services",
        getInTouch: "Get in Touch",
        servicesTitle: "Our <span class='accent-text'>Services</span>",
        servicesDesc: "We provide end-to-end solutions for all your digital needs",
        webDev: "Web Development",
        webDevDesc: "Custom websites and web applications built with the latest technologies.",
        mobileApps: "Mobile Apps",
        mobileAppsDesc: "Native and cross-platform mobile applications for iOS and Android.",
        uiuxDesign: "UI/UX Design",
        uiuxDesignDesc: "User-centered design focused on creating intuitive and engaging experiences.",
        digitalStrategy: "Digital Strategy",
        digitalStrategyDesc: "Comprehensive analysis and planning to maximize your digital presence.",
        projectsTitle: "Featured <span class='accent-text'>Projects</span>",
        projectsDesc: "Explore our latest work and success stories",
        all: "All",
        web: "Web",
        mobile: "Mobile",
        design: "Design",
        aboutTitle: "About <span class='accent-text'>Popzify</span>",
        aboutDesc1: "Founded in 2017, Popzify has grown into a leading digital agency specializing in creating exceptional web and mobile solutions. Our team of experienced developers, designers, and strategists works together to deliver innovative digital experiences.",
        aboutDesc2: "We believe in a collaborative approach, working closely with our clients to understand their unique needs and challenges. This allows us to create tailored solutions that drive real business results.",
        customSolutions: "Custom Solutions",
        latestTech: "Latest Technologies",
        responsiveDesign: "Responsive Design",
        ongoingSupport: "Ongoing Support",
        testimonialsTitle: "Client <span class='accent-text'>Testimonials</span>",
        testimonialsDesc: "What our clients say about working with us",
        contactTitle: "Get in <span class='accent-text'>Touch</span>",
        contactDesc: "Ready to start your project? Contact us today",
        location: "Our Location",
        emailUs: "Email Us",
        callUs: "Call Us",
        yourName: "Your Name",
        yourEmail: "Your Email",
        subject: "Subject",
        yourMessage: "Your Message",
        sendMessage: "Send Message",
        nameRequired: "Name is required",
        emailRequired: "Email is required",
        invalidEmail: "Please enter a valid email",
        subjectRequired: "Subject is required",
        messageRequired: "Message is required",
        messageShort: "Message must be at least 10 characters",
        sending: "Sending...",
        thankYou: "Thank you for your message! We will contact you soon.",
        scrollDown: "Scroll Down",
        backToTop: "Back to top",
        projects: "Completed Projects",
        clients: "Happy Clients",
        team: "Team Members",
        experience: "Years Experience",
        faqTitle:  "Frequently <span class='accent-text'>Asked</span> Questions",
    faqDesc : "Answers to questions our clients often ask",
    faqQuestion1 : "How long does it take to create a website?",
    faqAnswer1 : "The time required to create a website depends on the complexity of the project and our current workload. Generally, for a standard website, it takes around 14 to 30 days. For more complex projects with advanced functionalities, the development time may be longer. We define the exact timeframe after a detailed analysis of your requirements.",
    faqQuestion2 : "How long does mobile app development take?",
    faqAnswer2 : "Mobile app development is a more complex process that usually takes about 3 months. This timeframe includes design, development, testing, and implementation. More complex applications with advanced features may require a longer development period.",
    faqQuestion3 : "What payment methods do you accept?",
    faqAnswer3 : "We offer a wide range of payment options to make collaboration easier, including: Visa, Mastercard, American Express, Apple Pay, Google Pay, PayPal, USDT, and Bank transfers.",
    faqQuestion4 : "What programming languages and technologies do you use?",
    faqAnswer4 : "We use a wide range of modern technologies and programming languages, including: HTML5, CSS3, JavaScript, React, Vue.js, Angular, Node.js, PHP, Python, Java, and many more for both web and mobile development.",
    faqQuestion5 : "Do you provide support after project completion?",
    faqAnswer5 : "Yes, we offer ongoing support after project completion. Depending on your needs, we can offer different maintenance packages that include technical support, content updates, security updates, and performance optimization.",
    faqQuestion6 : "Do you redesign existing websites?",
    faqAnswer6 : "Yes, we specialize in redesigning and modernizing existing websites. Our team will analyze your current site, identify areas for improvement, and propose a comprehensive redesign plan that will enhance the visual appearance, functionality, and user experience.",
    faqQuestion7 : "What does the collaboration process look like?",
    faqAnswer7 : "Our collaboration process includes the following steps: Initial conversation, Analysis and planning, Design, Development, Testing, Launch, and Support.",
    frontend : "Front-end:",
    backend : "Back-end:",
    mobile : "Mobile:",
    databases : "Databases:",
    initialTalk: "Getting to know your business and needs",
planning: "Detailed project consideration and strategy definition",
design: "Creating visual identity and user interface",
development: "Programming and functionality implementation",
testing: "Quality and functionality verification",
launch: "Deploying the project to production environment",
support: "Continuous maintenance and technical support",
ourTeam: "Our Team",
teamSectionTitle: "Our <span class='accent-text'>Team</span>",
teamSectionDesc: "Meet the experts behind Popzify",
teamMember1Name: "Ivan Popović",
teamMember1Position: "Audio-video production & Marketing specialist",
teamMember1Bio: "Expert in audio-video production and digital marketing with over 8 years of industry experience.",
teamMember1Button: "Learn more",
teamMember2Name: "Stefan Popović",
teamMember2Position: "Developer & Sales strategist",
teamMember2Bio: "Expert in web development and sales strategies with over 10 years of experience in the IT sector.",
teamMember2Button: "Learn more",

// Add these to the existing 'en' object in translations
// For Ivan Popović's blog page
ivanBlogHeroTitle: "Ivan <span class='accent-text'>Popović</span>",
ivanBlogHeroSubtitle: "Specialist in Audio-Video Production and Digital Marketing",
ivanBlogAboutTitle: "About the <span class='accent-text'>Author</span>",
ivanBlogAboutDesc1: "Ivan Popović is an expert in audio-video production and digital marketing with over 8 years of industry experience. He graduated from the Faculty of Dramatic Arts, Production Department. His passion for visual storytelling and creative marketing solutions has led him to collaborate with numerous brands and agencies.",
ivanBlogAboutDesc2: "Through this blog, Ivan shares his knowledge, experience, and the latest trends from the world of multimedia production and digital marketing.",
ivanBlogPostsTitle: "Latest <span class='accent-text'>Articles</span>",
ivanBlogPostsDesc: "Learn more about audio-video production and digital marketing",
ivanBlogNewsletterTitle: "Subscribe to <span class='accent-text'>Newsletter</span>",
ivanBlogNewsletterDesc: "Stay updated with the latest trends and tips in audio-video production and digital marketing. Subscribe to our monthly newsletter and receive exclusive tips directly in your inbox.",
ivanBlogCategoriesTitle: "Blog <span class='accent-text'>Categories</span>",
ivanBlogCategoriesDesc: "Explore the topics that interest you",

// For Stefan Popović's blog page
stefanBlogHeroTitle: "Stefan <span class='accent-text'>Popović</span>",
stefanBlogHeroSubtitle: "Specialist in Programming and Sales Strategies",
stefanBlogAboutTitle: "About the <span class='accent-text'>Author</span>",
stefanBlogAboutDesc1: "Stefan Popović is an expert in web development and sales strategies with over 10 years of experience in the IT sector. He graduated in Computer Science from the Electrical Engineering Faculty and holds numerous certificates in digital business and sales.",
stefanBlogAboutDesc2: "By combining technical knowledge with an understanding of business processes, Stefan helps companies optimize their online presence and increase conversions. Through his blog, he shares practical advice on programming, e-commerce optimization, and sales strategies.",
stefanBlogPostsTitle: "Latest <span class='accent-text'>Articles</span>",
stefanBlogPostsDesc: "Learn more about programming and sales strategies",
stefanBlogCodeSnippetsTitle: "Useful <span class='accent-text'>Code Snippets</span>",
stefanBlogCodeSnippetsDesc: "Code examples that can improve your projects",
stefanBlogNewsletterTitle: "Subscribe to <span class='accent-text'>Newsletter</span>",
stefanBlogNewsletterDesc: "Want to receive the latest tips on programming and online sales? Subscribe to my newsletter and receive practical advice, code examples, and strategies you can implement immediately every month.",
stefanBlogCategoriesTitle: "Blog <span class='accent-text'>Categories</span>",
stefanBlogCategoriesDesc: "Explore the topics that interest you",
stefanBlogAuthorsTitle: "Our <span class='accent-text'>Authors</span>",
stefanBlogAuthorsDesc: "Meet the experts behind the Popzify blog",
blog: "Blog",

// Cookie consent prevodi
cookieTitle: "We use cookies",
cookieDesc: "We use cookies to enhance your browsing experience, personalize content and analyze traffic. By clicking 'Accept All', you consent to the use of all cookies. You can set your cookie preferences by clicking 'Cookie Settings'.",
cookieSettings: "Cookie Settings",
cookieAccept: "Accept All",
cookieSettingsTitle: "Cookie Settings",
necessaryCookies: "Necessary Cookies",
necessaryCookiesDesc: "These cookies are essential for the website to function and cannot be disabled.",
analyticsCookies: "Analytics Cookies",
analyticsCookiesDesc: "Help us understand how visitors interact with our website by collecting and reporting information anonymously.",
marketingCookies: "Marketing Cookies",
marketingCookiesDesc: "Track your activity across our website and other sites to provide you with relevant content and advertisements.",
saveSettings: "Save Settings"

    },
    sr: {
        home: "Početna",
        services: "Usluge",
        projects: "Projekti",
        about: "O nama",
        contact: "Kontakt",
        faq: "FAQ",
        heroTitle: "Kreiramo <span class='accent-text'>digitalna</span> iskustva",
        heroDesc: "Transformišemo ideje u savremena web i mobilna rešenja koja unapređuju vaš brend.",
        ourServices: "Naše usluge",
        getInTouch: "Kontaktirajte nas",
        servicesTitle: "Naše <span class='accent-text'>usluge</span>",
        servicesDesc: "Pružamo kompletna rešenja za sve vaše digitalne potrebe",
        webDev: "Web razvoj",
        webDevDesc: "Prilagođene web stranice i aplikacije izrađene najnovijim tehnologijama.",
        mobileApps: "Mobilne aplikacije",
        mobileAppsDesc: "Nativne i cross-platform mobilne aplikacije za iOS i Android.",
        uiuxDesign: "UI/UX dizajn",
        uiuxDesignDesc: "Dizajn usmeren na korisnika fokusiran na stvaranje intuitivnih i privlačnih iskustava.",
        digitalStrategy: "Digitalna strategija",
        digitalStrategyDesc: "Sveobuhvatna analiza i planiranje za maksimiziranje vašeg digitalnog prisustva.",
        projectsTitle: "Izdvojeni <span class='accent-text'>projekti</span>",
        projectsDesc: "Istražite naše najnovije radove i uspešne priče",
        all: "Sve",
        web: "Web",
        mobile: "Mobilno",
        design: "Dizajn",
        aboutTitle: "O <span class='accent-text'>Popzify</span>",
        aboutDesc1: "Osnovana 2017. godine, Popzify je izrastao u vodeću digitalnu agenciju specijalizovanu za kreiranje izuzetnih web i mobilnih rešenja. Naš tim iskusnih programera, dizajnera i stratega radi zajedno na pružanju inovativnih digitalnih iskustava.",
        aboutDesc2: "Verujemo u saradnički pristup, radeći blisko sa našim klijentima kako bismo razumeli njihove jedinstvene potrebe i izazove. To nam omogućava da kreiramo prilagođena rešenja koja donose prave poslovne rezultate.",
        customSolutions: "Prilagođena rešenja",
        latestTech: "Najnovije tehnologije",
        responsiveDesign: "Responzivan dizajn",
        ongoingSupport: "Kontinuirana podrška",
        testimonialsTitle: "Utisci <span class='accent-text'>klijenata</span>",
        testimonialsDesc: "Šta naši klijenti kažu o saradnji sa nama",
        contactTitle: "<span class='accent-text'>Kontaktirajte</span> nas",
        contactDesc: "Spremni ste da započnete projekat? Javite nam se danas",
        location: "Naša lokacija",
        emailUs: "Pošaljite nam e-mail",
        callUs: "Pozovite nas",
        yourName: "Vaše ime",
        yourEmail: "Vaš e-mail",
        subject: "Naslov",
        yourMessage: "Vaša poruka",
        sendMessage: "Pošalji poruku",
        nameRequired: "Ime je obavezno",
        emailRequired: "Email je obavezan",
        invalidEmail: "Unesite važeću email adresu",
        subjectRequired: "Naslov je obavezan",
        messageRequired: "Poruka je obavezna",
        messageShort: "Poruka mora imati najmanje 10 karaktera",
        sending: "Šalje se...",
        thankYou: "Hvala na poruci! Kontaktiraćemo vas uskoro.",
        scrollDown: "Pomerite nadole",
        backToTop: "Nazad na vrh",
        projects: "Završenih projekata",
        clients: "Zadovoljnih klijenata",
        team: "Članova tima",
        experience: "Godina iskustva",
        faqTitle: "Često <span class='accent-text'>Postavljena</span> Pitanja",
faqDesc: "Odgovori na pitanja koja nas najčešće pitaju klijenti",
faqQuestion1: "Koliko je vremena potrebno za izradu sajta?",
faqAnswer1: "Vreme izrade sajta zavisi od složenosti projekta i trenutnog obima posla. Generalno, za izradu standardnog sajta potrebno je od 14 do 30 dana. Za kompleksnije projekte sa naprednim funkcionalnostima, vreme izrade može biti duže. Tačan vremenski okvir definišemo nakon detaljne analize zahteva.",
faqQuestion2: "Koliko traje izrada mobilne aplikacije?",
faqAnswer2: "Izrada mobilne aplikacije je složeniji proces koji obično traje oko 3 meseca. Ovaj vremenski okvir uključuje dizajn, razvoj, testiranje i implementaciju. Složenije aplikacije sa naprednim funkcionalnostima mogu zahtevati duži period razvoja.",
faqQuestion3: "Koje načine plaćanja prihvatate?",
faqAnswer3: "Nudimo širok spektar opcija plaćanja kako bismo vam olakšali saradnju, uključujući: Visa, Mastercard, American Express, Apple Pay, Google Pay, PayPal, USDT i bankovni transfer.",
faqQuestion4: "Koje programske jezike i tehnologije koristite?",
faqAnswer4: "Koristimo širok spektar savremenih tehnologija i programskih jezika, uključujući: HTML5, CSS3, JavaScript, React, Vue.js, Angular, Node.js, PHP, Python, Java i mnoge druge za web i mobilni razvoj.",
faqQuestion5: "Da li pružate podršku nakon završetka projekta?",
faqAnswer5: "Da, nudimo kontinuiranu podršku nakon završetka projekta. U zavisnosti od vaših potreba, možemo vam ponuditi različite pakete održavanja koji uključuju tehničku podršku, ažuriranje sadržaja, sigurnosna ažuriranja i optimizaciju performansi.",
faqQuestion6: "Da li radite redizajn postojećih sajtova?",
faqAnswer6: "Da, specijalizovani smo za redizajn i modernizaciju postojećih sajtova. Naš tim će analizirati vaš trenutni sajt, identifikovati oblasti za poboljšanje i predložiti sveobuhvatan plan za redizajn koji će unaprediti vizuelni izgled, funkcionalnost i korisničko iskustvo.",
faqQuestion7: "Kako izgleda proces saradnje?",
faqAnswer7: "Naš proces saradnje obuhvata sledeće korake: Inicijalni razgovor, Analiza i planiranje, Dizajn, Razvoj, Testiranje, Lansiranje i Podrška.",
frontend: "Front-end:",
backend: "Back-end:",
mobile: "Mobilno:",
databases: "Baze podataka:",
initialTalk: "Upoznavanje sa vašim poslovanjem i potrebama",
planning: "Detaljno razmatranje projekta i definisanje strategije",
design: "Kreiranje vizuelnog identiteta i korisničkog interfejsa",
development: "Programiranje i implementacija funkcionalnosti",
testing: "Provera kvaliteta i funkcionalnosti",
launch: "Postavljanje projekta u produkciono okruženje",
support: "Kontinuirano održavanje i tehnička podrška",
ourTeam: "Naš tim",
teamSectionTitle: "Naš <span class='accent-text'>tim</span>",
teamSectionDesc: "Upoznajte stručnjake koji stoje iza Popzify-a",
teamMember1Name: "Ivan Popović",
teamMember1Position: "Audio-video produkcija & Marketing stručnjak",
teamMember1Bio: "Stručnjak za audio-video produkciju i digitalni marketing sa više od 8 godina iskustva u industriji.",
teamMember1Button: "Saznaj više",
teamMember2Name: "Stefan Popović",
teamMember2Position: "Programer & Prodajni strateg",
teamMember2Bio: "Stručnjak za web razvoj i prodajne strategije sa preko 10 godina iskustva u IT sektoru.",
teamMember2Button: "Saznaj više",
// Add these to the existing 'sr' object in translations
// For Ivan Popović's blog page
ivanBlogHeroTitle: "Ivan <span class='accent-text'>Popović</span>",
ivanBlogHeroSubtitle: "Specijalizant audio-video produkcije i digitalnog marketinga",
ivanBlogAboutTitle: "O <span class='accent-text'>autoru</span>",
ivanBlogAboutDesc1: "Ivan Popović je stručnjak za audio-video produkciju i digitalni marketing sa više od 8 godina iskustva u industriji. Diplomirao je na Fakultetu dramskih umetnosti, smer Produkcija. Njegova strast za vizuelnim pričanjem priča i kreativnim marketinškim rešenjima dovela ga je do saradnje sa brojnim brendovima i agencijama.",
ivanBlogAboutDesc2: "Kroz ovaj blog, Ivan deli svoje znanje, iskustvo i najnovije trendove iz sveta multimedijalne produkcije i digitalnog marketinga.",
ivanBlogPostsTitle: "Najnoviji <span class='accent-text'>članci</span>",
ivanBlogPostsDesc: "Saznajte više o audio-video produkciji i digitalnom marketingu",
ivanBlogNewsletterTitle: "Prijava na <span class='accent-text'>newsletter</span>",
ivanBlogNewsletterDesc: "Ostanite u toku sa najnovijim trendovima i savetima iz oblasti audio-video produkcije i digitalnog marketinga. Prijavite se na mesečni newsletter i dobijte ekskluzivne savete direktno u vaš inbox.",
ivanBlogCategoriesTitle: "Kategorije <span class='accent-text'>bloga</span>",
ivanBlogCategoriesDesc: "Istražite teme koje vas interesuju",

// For Stefan Popović's blog page
stefanBlogHeroTitle: "Stefan <span class='accent-text'>Popović</span>",
stefanBlogHeroSubtitle: "Specijalizant programiranja i prodajnih strategija",
stefanBlogAboutTitle: "O <span class='accent-text'>autoru</span>",
stefanBlogAboutDesc1: "Stefan Popović je stručnjak za web razvoj i prodajne strategije sa preko 10 godina iskustva u IT sektoru. Diplomirao je Računarske nauke na Elektrotehničkom fakultetu i poseduje brojne sertifikate iz oblasti digitalnog poslovanja i prodaje.",
stefanBlogAboutDesc2: "Kombinujući tehničko znanje sa razumevanjem poslovnih procesa, Stefan pomaže kompanijama da optimizuju svoje online prisustvo i povećaju konverzije. Kroz svoj blog, deli praktične savete iz oblasti programiranja, e-commerce optimizacije i prodajnih strategija.",
stefanBlogPostsTitle: "Najnoviji <span class='accent-text'>članci</span>",
stefanBlogPostsDesc: "Saznajte više o programiranju i prodajnim strategijama",
stefanBlogCodeSnippetsTitle: "Korisni <span class='accent-text'>code snippets</span>",
stefanBlogCodeSnippetsDesc: "Primeri koda koji mogu unaprediti vaše projekte",
stefanBlogNewsletterTitle: "Prijava na <span class='accent-text'>newsletter</span>",
stefanBlogNewsletterDesc: "Želite li da dobijate najnovije savete iz oblasti programiranja i online prodaje? Prijavite se na moj newsletter i svakog meseca dobijajte praktične savete, kod primere i strategije koje možete odmah implementirati.",
stefanBlogCategoriesTitle: "Kategorije <span class='accent-text'>bloga</span>",
stefanBlogCategoriesDesc: "Istražite teme koje vas interesuju",
stefanBlogAuthorsTitle: "Naši <span class='accent-text'>autori</span>",
stefanBlogAuthorsDesc: "Upoznajte stručnjake koji stoje iza Popzify bloga",
blog: "Blog",

 // Cookie consent prevodi
 cookieTitle: "Koristimo kolačiće",
 cookieDesc: "Koristimo kolačiće kako bismo poboljšali vaše iskustvo na našoj web stranici, personalizovali sadržaj i analizirali promet. Klikom na 'Prihvati sve' pristajete na upotrebu svih kolačića. Možete postaviti svoje postavke kolačića klikom na 'Postavke kolačića'.",
 cookieSettings: "Postavke kolačića",
 cookieAccept: "Prihvati sve",
 cookieSettingsTitle: "Postavke kolačića",
 necessaryCookies: "Neophodni kolačići",
 necessaryCookiesDesc: "Ovi kolačići su neophodni za funkcionisanje web stranice i ne mogu se isključiti.",
 analyticsCookies: "Analitički kolačići",
 analyticsCookiesDesc: "Pomažu nam da razumemo kako korisnici koriste našu web stranicu, prikupljajući i izveštavajući informacije anonimno.",
 marketingCookies: "Marketinški kolačići",
 marketingCookiesDesc: "Prate vaše aktivnosti na našoj web stranici i drugim sajtovima kako bismo vam prikazali relevantan sadržaj i oglase.",
 saveSettings: "Sačuvaj postavke"
}
};

let currentLang = 'en'; // Default language

// Function to update page content based on selected language
function updateLanguage(lang) {
    try {
        currentLang = lang;
        document.documentElement.setAttribute('lang', lang);

        // Update language selector
        const langSelector = document.querySelector('.lang-selector');
        if (langSelector) {
            langSelector.setAttribute('data-lang', lang);
        }

        // Update navigation links
        const navLinks = {
            home: document.querySelector('.nav-menu a[data-nav="home"]'),
            services: document.querySelector('.nav-menu a[data-nav="services"]'),
            ourTeam: document.querySelector('.nav-menu a[data-nav="ourTeam"]'),
            projects: document.querySelector('.nav-menu a[data-nav="projects"]'),
            about: document.querySelector('.nav-menu a[data-nav="about"]'),
            faq: document.querySelector('.nav-menu a[data-nav="faq"]'),
            blog: document.querySelector('.nav-menu a[data-nav="blog"]'),
            contact: document.querySelector('.nav-menu a[data-nav="contact"]')
        };

        // Update only the links that exist
        if (navLinks.home) navLinks.home.textContent = translations[lang].home;
        if (navLinks.services) navLinks.services.textContent = translations[lang].services;
        if (navLinks.ourTeam) navLinks.ourTeam.textContent = translations[lang].ourTeam;
        if (navLinks.projects) navLinks.projects.textContent = translations[lang].projects;
        if (navLinks.about) navLinks.about.textContent = translations[lang].about;
        if (navLinks.faq) navLinks.faq.textContent = translations[lang].faq;
        if (navLinks.blog) navLinks.blog.textContent = translations[lang].blog;
        if (navLinks.contact) navLinks.contact.textContent = translations[lang].contact;

        // Update team section if it exists
        if (document.querySelector('#team')) {
            const teamHeader = document.querySelector('#team .section-header h2');
            const teamDesc = document.querySelector('#team .section-header p');
            
            if (teamHeader) teamHeader.innerHTML = translations[lang].teamSectionTitle;
            if (teamDesc) teamDesc.textContent = translations[lang].teamSectionDesc;
            
            // Translate team member details
            const teamMembers = document.querySelectorAll('.team-member');
            if (teamMembers.length >= 2) {
                // First team member (Ivan)
                try {
                    const member1 = teamMembers[0];
                    if (member1.querySelector('h3')) member1.querySelector('h3').textContent = translations[lang].teamMember1Name;
                    if (member1.querySelector('.member-position')) member1.querySelector('.member-position').textContent = translations[lang].teamMember1Position;
                    if (member1.querySelector('.member-bio')) member1.querySelector('.member-bio').textContent = translations[lang].teamMember1Bio;
                    if (member1.querySelector('.btn')) member1.querySelector('.btn').textContent = translations[lang].teamMember1Button;
                } catch (error) {
                    console.error("Error updating team member 1:", error);
                }
                
                // Second team member (Stefan)
                try {
                    const member2 = teamMembers[1];
                    if (member2.querySelector('h3')) member2.querySelector('h3').textContent = translations[lang].teamMember2Name;
                    if (member2.querySelector('.member-position')) member2.querySelector('.member-position').textContent = translations[lang].teamMember2Position;
                    if (member2.querySelector('.member-bio')) member2.querySelector('.member-bio').textContent = translations[lang].teamMember2Bio;
                    if (member2.querySelector('.btn')) member2.querySelector('.btn').textContent = translations[lang].teamMember2Button;
                } catch (error) {
                    console.error("Error updating team member 2:", error);
                }
            }
        }

        // Update hero section
        try {
            const heroContent = document.querySelector('.hero-content');
            if (heroContent) {
                const heroTitle = heroContent.querySelector('h1');
                const heroDesc = heroContent.querySelector('p');
                const heroPrimary = heroContent.querySelector('.btn-primary');
                const heroSecondary = heroContent.querySelector('.btn-secondary');
                
                if (heroTitle) heroTitle.innerHTML = translations[lang].heroTitle;
                if (heroDesc) heroDesc.textContent = translations[lang].heroDesc;
                if (heroPrimary) heroPrimary.textContent = translations[lang].ourServices;
                if (heroSecondary) heroSecondary.textContent = translations[lang].getInTouch;
            }
            
            const heroScroll = document.querySelector('.hero-scroll p');
            if (heroScroll) heroScroll.textContent = translations[lang].scrollDown;
        } catch (error) {
            console.error("Error updating hero section:", error);
        }

        // Update services section
        try {
            const services = document.querySelector('#services');
            if (services) {
                const servicesHeader = services.querySelector('.section-header h2');
                const servicesDesc = services.querySelector('.section-header p');
                
                if (servicesHeader) servicesHeader.innerHTML = translations[lang].servicesTitle;
                if (servicesDesc) servicesDesc.textContent = translations[lang].servicesDesc;

                const serviceCards = services.querySelectorAll('.service-card');
                if (serviceCards.length >= 4) {
                    if (serviceCards[0].querySelector('h3')) serviceCards[0].querySelector('h3').textContent = translations[lang].webDev;
                    if (serviceCards[0].querySelector('p')) serviceCards[0].querySelector('p').textContent = translations[lang].webDevDesc;
                    
                    if (serviceCards[1].querySelector('h3')) serviceCards[1].querySelector('h3').textContent = translations[lang].mobileApps;
                    if (serviceCards[1].querySelector('p')) serviceCards[1].querySelector('p').textContent = translations[lang].mobileAppsDesc;
                    
                    if (serviceCards[2].querySelector('h3')) serviceCards[2].querySelector('h3').textContent = translations[lang].uiuxDesign;
                    if (serviceCards[2].querySelector('p')) serviceCards[2].querySelector('p').textContent = translations[lang].uiuxDesignDesc;
                    
                    if (serviceCards[3].querySelector('h3')) serviceCards[3].querySelector('h3').textContent = translations[lang].digitalStrategy;
                    if (serviceCards[3].querySelector('p')) serviceCards[3].querySelector('p').textContent = translations[lang].digitalStrategyDesc;
                }
            }
        } catch (error) {
            console.error("Error updating services section:", error);
        }

        // Update projects section
        try {
            const projects = document.querySelector('#projects');
            if (projects) {
                const projectsHeader = projects.querySelector('.section-header h2');
                const projectsDesc = projects.querySelector('.section-header p');
                
                if (projectsHeader) projectsHeader.innerHTML = translations[lang].projectsTitle;
                if (projectsDesc) projectsDesc.textContent = translations[lang].projectsDesc;
                
                const filterBtns = projects.querySelectorAll('.filter-btn');
                if (filterBtns.length >= 4) {
                    filterBtns[0].textContent = translations[lang].all;
                    filterBtns[1].textContent = translations[lang].web;
                    filterBtns[2].textContent = translations[lang].mobile;
                    filterBtns[3].textContent = translations[lang].design;
                }
            }
        } catch (error) {
            console.error("Error updating projects section:", error);
        }

        // Update stats section
        try {
            const statItems = document.querySelectorAll('.stat-item p');
            if (statItems.length >= 4) {
                statItems[0].textContent = translations[lang].projects;
                statItems[1].textContent = translations[lang].clients;
                statItems[2].textContent = translations[lang].team;
                statItems[3].textContent = translations[lang].experience;
            }
        } catch (error) {
            console.error("Error updating stats section:", error);
        }

        // Update about section
        try {
            const about = document.querySelector('#about');
            if (about) {
                const aboutHeader = about.querySelector('.section-header h2');
                if (aboutHeader) aboutHeader.innerHTML = translations[lang].aboutTitle;
                
                const aboutParagraphs = about.querySelectorAll('p');
                if (aboutParagraphs.length >= 2) {
                    aboutParagraphs[0].textContent = translations[lang].aboutDesc1;
                    aboutParagraphs[1].textContent = translations[lang].aboutDesc2;
                }
                
                const features = about.querySelectorAll('.feature p');
                if (features.length >= 4) {
                    features[0].textContent = translations[lang].customSolutions;
                    features[1].textContent = translations[lang].latestTech;
                    features[2].textContent = translations[lang].responsiveDesign;
                    features[3].textContent = translations[lang].ongoingSupport;
                }
            }
        } catch (error) {
            console.error("Error updating about section:", error);
        }

        // Update testimonials section
        try {
            const testimonials = document.querySelector('.testimonials');
            if (testimonials) {
                const testimonialsHeader = testimonials.querySelector('.section-header h2');
                const testimonialsDesc = testimonials.querySelector('.section-header p');
                
                if (testimonialsHeader) testimonialsHeader.innerHTML = translations[lang].testimonialsTitle;
                if (testimonialsDesc) testimonialsDesc.textContent = translations[lang].testimonialsDesc;
            }
        } catch (error) {
            console.error("Error updating testimonials section:", error);
        }

        // Update contact section
        try {
            const contact = document.querySelector('#contact');
            if (contact) {
                const contactHeader = contact.querySelector('.section-header h2');
                const contactDesc = contact.querySelector('.section-header p');
                
                if (contactHeader) contactHeader.innerHTML = translations[lang].contactTitle;
                if (contactDesc) contactDesc.textContent = translations[lang].contactDesc;
                
                const contactHeadings = contact.querySelectorAll('.contact-text h3');
                if (contactHeadings.length >= 3) {
                    contactHeadings[0].textContent = translations[lang].location;
                    contactHeadings[1].textContent = translations[lang].emailUs;
                    contactHeadings[2].textContent = translations[lang].callUs;
                }
                
                const nameInput = document.getElementById('name');
                const emailInput = document.getElementById('email');
                const subjectInput = document.getElementById('subject');
                const messageInput = document.getElementById('message');
                const submitBtn = contact.querySelector('.contact-form button');
                
                if (nameInput) nameInput.setAttribute('placeholder', translations[lang].yourName);
                if (emailInput) emailInput.setAttribute('placeholder', translations[lang].yourEmail);
                if (subjectInput) subjectInput.setAttribute('placeholder', translations[lang].subject);
                if (messageInput) messageInput.setAttribute('placeholder', translations[lang].yourMessage);
                if (submitBtn) submitBtn.textContent = translations[lang].sendMessage;
            }
        } catch (error) {
            console.error("Error updating contact section:", error);
        }

        // Update back to top button
        try {
            const backToTopBtn = document.querySelector('.back-to-top');
            if (backToTopBtn) {
                backToTopBtn.setAttribute('title', translations[lang].backToTop);
            }
        } catch (error) {
            console.error("Error updating back to top button:", error);
        }

        // Update blog content if we're on a blog page
        updateBlogPageTranslations(lang);

        // Save language preference
        localStorage.setItem('preferredLanguage', lang);
    } catch (error) {
        console.error("Error in updateLanguage function:", error);
    }
}

// Trigger an event when language changes
function updateLanguageWithEvents(lang) {
    updateLanguage(lang);
    const event = new CustomEvent('languageChanged', { detail: { language: lang } });
    document.dispatchEvent(event);
}

function updateBlogPageTranslations(lang) {

    try {
        // Ivan's blog page
        const ivanBlogHero = document.querySelector('.blog-hero:not(.tech-hero)');
        if (ivanBlogHero) {
            
            const heroTitle = ivanBlogHero.querySelector('.blog-author h1');
            const heroSubtitle = ivanBlogHero.querySelector('.author-info p');

            if (heroTitle) heroTitle.innerHTML = translations[lang].ivanBlogHeroTitle;
            if (heroSubtitle) heroSubtitle.textContent = translations[lang].ivanBlogHeroSubtitle;

            const aboutSection = document.querySelector('.blog-about');
            if (aboutSection) {
                const aboutTitle = aboutSection.querySelector('h2');
                const aboutParagraphs = aboutSection.querySelectorAll('.about-text p');
                
                if (aboutTitle) aboutTitle.innerHTML = translations[lang].ivanBlogAboutTitle;
                if (aboutParagraphs.length >= 2) {
                    aboutParagraphs[0].textContent = translations[lang].ivanBlogAboutDesc1;
                    aboutParagraphs[1].textContent = translations[lang].ivanBlogAboutDesc2;
                }
            }

            const postsSection = document.querySelector('.blog-posts .section-header');
            if (postsSection) {
                const postsTitle = postsSection.querySelector('h2');
                const postsDesc = postsSection.querySelector('p');
                
                if (postsTitle) postsTitle.innerHTML = translations[lang].ivanBlogPostsTitle;
                if (postsDesc) postsDesc.textContent = translations[lang].ivanBlogPostsDesc;
            }

            const newsletterSection = document.querySelector('.newsletter-section');
            if (newsletterSection) {
                const newsletterTitle = newsletterSection.querySelector('h2');
                const newsletterDesc = newsletterSection.querySelector('p');
                
                if (newsletterTitle) newsletterTitle.innerHTML = translations[lang].ivanBlogNewsletterTitle;
                if (newsletterDesc) newsletterDesc.textContent = translations[lang].ivanBlogNewsletterDesc;
            }

            const categoriesSection = document.querySelector('.categories-section .section-header');
            if (categoriesSection) {
                const categoriesTitle = categoriesSection.querySelector('h2');
                const categoriesDesc = categoriesSection.querySelector('p');
                
                if (categoriesTitle) categoriesTitle.innerHTML = translations[lang].ivanBlogCategoriesTitle;
                if (categoriesDesc) categoriesDesc.textContent = translations[lang].ivanBlogCategoriesDesc;
            }
        }

        // Stefan's blog page
        const stefanBlogHero = document.querySelector('.blog-hero.tech-hero');
        if (stefanBlogHero) {
            
            const heroTitle = stefanBlogHero.querySelector('.blog-author h1');
            const heroSubtitle = stefanBlogHero.querySelector('.author-info p');

            if (heroTitle) heroTitle.innerHTML = translations[lang].stefanBlogHeroTitle;
            if (heroSubtitle) heroSubtitle.textContent = translations[lang].stefanBlogHeroSubtitle;

            const aboutSection = document.querySelector('.blog-about');
            if (aboutSection) {
                const aboutTitle = aboutSection.querySelector('h2');
                const aboutParagraphs = aboutSection.querySelectorAll('.about-text p');
                
                if (aboutTitle) aboutTitle.innerHTML = translations[lang].stefanBlogAboutTitle;
                if (aboutParagraphs.length >= 2) {
                    aboutParagraphs[0].textContent = translations[lang].stefanBlogAboutDesc1;
                    aboutParagraphs[1].textContent = translations[lang].stefanBlogAboutDesc2;
                }
            }

            const postsSection = document.querySelector('.blog-posts .section-header');
            if (postsSection) {
                const postsTitle = postsSection.querySelector('h2');
                const postsDesc = postsSection.querySelector('p');
                
                if (postsTitle) postsTitle.innerHTML = translations[lang].stefanBlogPostsTitle;
                if (postsDesc) postsDesc.textContent = translations[lang].stefanBlogPostsDesc;
            }

            const codeSnippetsSection = document.querySelector('.code-snippet-section .section-header');
            if (codeSnippetsSection) {
                const snippetsTitle = codeSnippetsSection.querySelector('h2');
                const snippetsDesc = codeSnippetsSection.querySelector('p');
                
                if (snippetsTitle) snippetsTitle.innerHTML = translations[lang].stefanBlogCodeSnippetsTitle;
                if (snippetsDesc) snippetsDesc.textContent = translations[lang].stefanBlogCodeSnippetsDesc;
            }

            const newsletterSection = document.querySelector('.newsletter-section');
            if (newsletterSection) {
                const newsletterTitle = newsletterSection.querySelector('h2');
                const newsletterDesc = newsletterSection.querySelector('p');
                
                if (newsletterTitle) newsletterTitle.innerHTML = translations[lang].stefanBlogNewsletterTitle;
                if (newsletterDesc) newsletterDesc.textContent = translations[lang].stefanBlogNewsletterDesc;
            }

            const categoriesSection = document.querySelector('.categories-section .section-header');
            if (categoriesSection) {
                const categoriesTitle = categoriesSection.querySelector('h2');
                const categoriesDesc = categoriesSection.querySelector('p');
                
                if (categoriesTitle) categoriesTitle.innerHTML = translations[lang].stefanBlogCategoriesTitle;
                if (categoriesDesc) categoriesDesc.textContent = translations[lang].stefanBlogCategoriesDesc;
            }

            const authorsSection = document.querySelector('.blog-authors .section-header');
            if (authorsSection) {
                const authorsTitle = authorsSection.querySelector('h2');
                const authorsDesc = authorsSection.querySelector('p');
                
                if (authorsTitle) authorsTitle.innerHTML = translations[lang].stefanBlogAuthorsTitle;
                if (authorsDesc) authorsDesc.textContent = translations[lang].stefanBlogAuthorsDesc;
            }
        }
    } catch (error) {
        console.error('Error translating blog pages:', error);
    }
}

// Initialize interactive elements when DOM is loaded
document.addEventListener('DOMContentLoaded', function() {
    // Add loading="lazy" attribute to images for better performance
    document.querySelectorAll('img:not([loading])').forEach(img => {
        img.setAttribute('loading', 'lazy');
    });
 
    // Cookie Consent Manager
    const CookieConsent = {
        // Cookie names
        cookieNames: {
            consent: 'popzify_cookie_consent',
            analytics: 'popzify_allow_analytics',
            marketing: 'popzify_allow_marketing'
        },
        
        // DOM elements
        elements: {
            banner: document.querySelector('.cookie-banner'),
            acceptBtn: document.querySelector('.cookie-btn-accept'),
            settingsBtn: document.querySelector('.cookie-btn-settings'),
            modal: document.querySelector('.cookie-modal'),
            closeModalBtn: document.querySelector('.cookie-modal-close'),
            saveSettingsBtn: document.querySelector('.cookie-modal-save'),
            analyticsCheckbox: document.getElementById('analytics-cookies'),
            marketingCheckbox: document.getElementById('marketing-cookies'),
            cookieTextElements: document.querySelectorAll('[data-cookie-text]')
        },
        
        // Initialize the cookie consent management
        init() {
            if (!this.elements.banner) {
                return; // Exit if elements aren't found
            }
 
            // Update cookie texts when language changes
            document.querySelectorAll('.lang-btn').forEach(btn => {
                btn.addEventListener('click', () => {
                    setTimeout(() => {
                        this.updateCookieTexts();
                    }, 100);
                });
            });
            
            // Check if consent has already been given
            if (!this.getCookie(this.cookieNames.consent)) {
                // Show the banner if no consent has been given yet
                setTimeout(() => {
                    this.elements.banner.classList.add('active');
                }, 1000);
            } else {
                // Load allowed services based on saved preferences
                this.loadAllowedServices();
            }
            
            // Set up event listeners
            this.setupEventListeners();
            
            // Load saved preferences into checkboxes
            this.loadSavedPreferences();
        },
        
        // Update cookie-related texts based on current language
        updateCookieTexts() {
            this.elements.cookieTextElements.forEach(element => {
                const textKey = element.getAttribute('data-cookie-text');
                if (translations[currentLang][textKey]) {
                    element.textContent = translations[currentLang][textKey];
                }
            });
        },
        
        // Set up event listeners for buttons
        setupEventListeners() {
            // Accept all cookies
            this.elements.acceptBtn.addEventListener('click', () => {
                this.acceptAll();
                this.hideBanner();
            });
            
            // Open settings modal
            this.elements.settingsBtn.addEventListener('click', () => {
                this.showModal();
            });
            
            // Close modal
            this.elements.closeModalBtn.addEventListener('click', () => {
                this.hideModal();
            });
            
            // Save settings
            this.elements.saveSettingsBtn.addEventListener('click', () => {
                this.saveSettings();
                this.hideModal();
                this.hideBanner();
            });
            
            // Close modal when clicking outside
            window.addEventListener('click', (e) => {
                if (e.target === this.elements.modal) {
                    this.hideModal();
                }
            });
            
            // Update cookie texts when language changes
            document.addEventListener('languageChanged', () => {
                this.updateCookieTexts();
            });
        },
        
        // Load saved preferences into checkboxes
        loadSavedPreferences() {
            const allowAnalytics = this.getCookie(this.cookieNames.analytics) === 'true';
            const allowMarketing = this.getCookie(this.cookieNames.marketing) === 'true';
            
            if (this.elements.analyticsCheckbox) {
                this.elements.analyticsCheckbox.checked = allowAnalytics;
            }
            
            if (this.elements.marketingCheckbox) {
                this.elements.marketingCheckbox.checked = allowMarketing;
            }
        },
        
        // Accept all cookies
        acceptAll() {
            this.setCookie(this.cookieNames.consent, 'true', 365);
            this.setCookie(this.cookieNames.analytics, 'true', 365);
            this.setCookie(this.cookieNames.marketing, 'true', 365);
            
            this.loadAllowedServices();
        },
        
        // Save user's cookie settings
        saveSettings() {
            const allowAnalytics = this.elements.analyticsCheckbox.checked;
            const allowMarketing = this.elements.marketingCheckbox.checked;
            
            this.setCookie(this.cookieNames.consent, 'true', 365);
            this.setCookie(this.cookieNames.analytics, allowAnalytics.toString(), 365);
            this.setCookie(this.cookieNames.marketing, allowMarketing.toString(), 365);
            
            this.loadAllowedServices();
        },
        
        // Load services based on saved preferences
        loadAllowedServices() {
            const allowAnalytics = this.getCookie(this.cookieNames.analytics) === 'true';
            const allowMarketing = this.getCookie(this.cookieNames.marketing) === 'true';
            
            if (allowAnalytics) {
                this.loadAnalytics();
            }
            
            if (allowMarketing) {
                this.loadMarketing();
            }
        },
        
        // Load analytics services (e.g., Google Analytics)
        loadAnalytics() {
            // Here you would typically load your analytics scripts
        },
        
        // Load marketing services (e.g., Facebook Pixel)
        loadMarketing() {
            // Here you would typically load your marketing scripts
        },
        
        // Show the cookie settings modal
        showModal() {
            this.elements.modal.classList.add('active');
            document.body.style.overflow = 'hidden';
        },
        
        // Hide the cookie settings modal
        hideModal() {
            this.elements.modal.classList.remove('active');
            document.body.style.overflow = '';
        },
        
        // Hide the cookie banner
        hideBanner() {
            this.elements.banner.classList.remove('active');
        },
        
        // Helper function to set a cookie
        setCookie(name, value, days) {
            const date = new Date();
            date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
            const expires = '; expires=' + date.toUTCString();
            document.cookie = name + '=' + value + expires + '; path=/; SameSite=Lax';
        },
        
        // Helper function to get a cookie value
        getCookie(name) {
            const nameEQ = name + '=';
            const ca = document.cookie.split(';');
            for (let i = 0; i < ca.length; i++) {
                let c = ca[i];
                while (c.charAt(0) === ' ') c = c.substring(1, c.length);
                if (c.indexOf(nameEQ) === 0) return c.substring(nameEQ.length, c.length);
            }
            return null;
        }
    };
 
    // Navbar scroll effect
    const navbar = document.querySelector('.navbar');
    const hamburger = document.querySelector('.hamburger');
    const navMenu = document.querySelector('.nav-menu');
 
    window.addEventListener('scroll', function() {
        if (window.scrollY > 50) {
            navbar.classList.add('scrolled');
        } else {
            navbar.classList.remove('scrolled');
        }
    });
 
    // Mobile menu toggle
    hamburger.addEventListener('click', function() {
        hamburger.classList.toggle('active');
        navMenu.classList.toggle('active');
    });
 
    // Close mobile menu when clicking a link
    document.querySelectorAll('.nav-menu a').forEach(link => {
        link.addEventListener('click', function() {
            hamburger.classList.remove('active');
            navMenu.classList.remove('active');
        });
    });
 
    // Active link on scroll
    const sections = document.querySelectorAll('section[id]');
    const navLinks = document.querySelectorAll('.nav-menu a[href^="#"]');
 
    function updateActiveNavLink() {
        let current = '';
 
        // Find current section based on scroll
        sections.forEach(section => {
            const sectionTop = section.offsetTop;
            const sectionHeight = section.clientHeight;
            const scrollPosition = window.scrollY + 100; // Add offset for better tracking
 
            if (scrollPosition >= sectionTop && scrollPosition < sectionTop + sectionHeight) {
                current = section.getAttribute('id');
            }
        });
 
        // If at the top of the page, set "Home" as active
        if (window.scrollY < 200) {
            current = 'hero';
        }
 
        navLinks.forEach(link => {
            link.classList.remove('active');
            
            // Extract the section ID from the href attribute
            const href = link.getAttribute('href').split('#')[1] || '';
            
            if (href === current) {
                link.classList.add('active');
            }
        });
    }
 
    // Run updateActiveNavLink on scroll
    window.addEventListener('scroll', updateActiveNavLink);
 
    // Run updateActiveNavLink once after page load
    updateActiveNavLink();
 
    // Project filter
    const filterBtns = document.querySelectorAll('.filter-btn');
    const projectCards = document.querySelectorAll('.project-card');
 
    filterBtns.forEach(btn => {
        btn.addEventListener('click', function() {
            // Remove active class from all buttons
            filterBtns.forEach(btn => btn.classList.remove('active'));
    
            // Add active class to clicked button
            this.classList.add('active');
    
            // Get filter value
            const filterValue = this.getAttribute('data-filter');
    
            projectCards.forEach(card => {
                // Get card categories as an array
                const categories = card.getAttribute('data-category').split(' ');
                
                if (filterValue === 'all' || categories.includes(filterValue)) {
                    // Show this card with animation
                    card.style.display = 'flex'; // Koristi flex umesto block
                    setTimeout(() => {
                        card.style.opacity = '1';
                        card.style.transform = 'translateY(0)';
                    }, 10);
                } else {
                    // Hide this card with animation
                    card.style.opacity = '0';
                    card.style.transform = 'translateY(20px)';
                    setTimeout(() => {
                        card.style.display = 'none';
                    }, 300);
                }
            });
        });
    });
 
    // Counter animation
    const counters = document.querySelectorAll('.counter');
    const speed = 200;
 
    function animateCounter() {
        counters.forEach(counter => {
            const target = +counter.getAttribute('data-target');
            const count = +counter.innerText;
 
            const increment = target / speed;
 
            if (count < target) {
                counter.innerText = Math.ceil(count + increment);
                setTimeout(animateCounter, 1);
            } else {
                counter.innerText = target;
            }
        });
    }
 
    // Run counter animation when stats section is in view
    const statsSection = document.querySelector('.stats');
 
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                animateCounter();
                observer.unobserve(entry.target);
            }
        });
    }, { threshold: 0.5 });
 
    if (statsSection) {
        observer.observe(statsSection);
    }
 
    // Testimonial slider
    const testimonialSlides = document.querySelectorAll('.testimonial-slide');
    const dots = document.querySelectorAll('.dot');
    const prevBtn = document.querySelector('.prev-btn');
    const nextBtn = document.querySelector('.next-btn');
    let currentSlide = 0;
 
    function showSlide(n) {
        // Hide all slides
        testimonialSlides.forEach(slide => {
            slide.classList.remove('active');
        });
 
        // Remove active class from all dots
        dots.forEach(dot => {
            dot.classList.remove('active');
        });
 
        // Show the current slide and dot
        testimonialSlides[n].classList.add('active');
        dots[n].classList.add('active');
    }
 
    function nextSlide() {
        currentSlide++;
        if (currentSlide >= testimonialSlides.length) {
            currentSlide = 0;
        }
        showSlide(currentSlide);
    }
 
    function prevSlide() {
        currentSlide--;
        if (currentSlide < 0) {
            currentSlide = testimonialSlides.length - 1;
        }
        showSlide(currentSlide);
    }
 
    // Add touch swipe for testimonial slider
    let touchStartX = 0;
    let touchEndX = 0;
 
    const testimonialSlider = document.querySelector('.testimonial-slider');
    if (testimonialSlider) {
        testimonialSlider.addEventListener('touchstart', e => {
            touchStartX = e.changedTouches[0].screenX;
        });
 
        testimonialSlider.addEventListener('touchend', e => {
            touchEndX = e.changedTouches[0].screenX;
            handleSwipe();
        });
    }
 
    function handleSwipe() {
        if (touchEndX < touchStartX - 50) {
            nextSlide(); // Swipe left, next slide
        }
        if (touchEndX > touchStartX + 50) {
            prevSlide(); // Swipe right, previous slide
        }
    }
 
    // Event listeners for testimonial controls
    if (prevBtn && nextBtn) {
        prevBtn.addEventListener('click', prevSlide);
        nextBtn.addEventListener('click', nextSlide);
 
        // Automatically change slide every 5 seconds
        const slideInterval = setInterval(nextSlide, 5000);
 
        // Pause auto-slide when user interacts with controls
        testimonialSlider.addEventListener('mouseenter', () => {
            clearInterval(slideInterval);
        });
 
        // Add click event to dots
        dots.forEach((dot, index) => {
            dot.addEventListener('click', () => {
                currentSlide = index;
                showSlide(currentSlide);
            });
        });
    }
 
    // Form validation function
    function validateForm() {
        let isValid = true;
 
        // Validate name
        const nameInput = document.getElementById('name');
        const nameError = document.getElementById('name-error');
        if (nameInput && nameError) {
            if (nameInput.value.trim() === '') {
                nameError.textContent = translations[currentLang].nameRequired;
                nameInput.classList.add('error');
                isValid = false;
            } else {
                nameError.textContent = '';
                nameInput.classList.remove('error');
            }
        }
 
        // Validate email
        const emailInput = document.getElementById('email');
        const emailError = document.getElementById('email-error');
        if (emailInput && emailError) {
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (emailInput.value.trim() === '') {
                emailError.textContent = translations[currentLang].emailRequired;
                emailInput.classList.add('error');
                isValid = false;
            } else if (!emailRegex.test(emailInput.value)) {
                emailError.textContent = translations[currentLang].invalidEmail;
                emailInput.classList.add('error');
                isValid = false;
            } else {
                emailError.textContent = '';
                emailInput.classList.remove('error');
            }
        }
 
        // Validate subject
        const subjectInput = document.getElementById('subject');
        const subjectError = document.getElementById('subject-error');
        if (subjectInput && subjectError) {
            if (subjectInput.value.trim() === '') {
                subjectError.textContent = translations[currentLang].subjectRequired;
                subjectInput.classList.add('error');
                isValid = false;
            } else {
                subjectError.textContent = '';
                subjectInput.classList.remove('error');
            }
        }
 
        // Validate message
        const messageInput = document.getElementById('message');
        const messageError = document.getElementById('message-error');
        if (messageInput && messageError) {
            if (messageInput.value.trim() === '') {
                messageError.textContent = translations[currentLang].messageRequired;
                messageInput.classList.add('error');
                isValid = false;
            } else if (messageInput.value.trim().length < 10) {
                messageError.textContent = translations[currentLang].messageShort;
                messageInput.classList.add('error');
                isValid = false;
            } else {
                messageError.textContent = '';
                messageInput.classList.remove('error');
            }
        }
 
        return isValid;
    }
 
    // First make sure that the error elements exist
    const contactForm = document.getElementById('contactForm');
    if (contactForm) {
        // Create error elements if they don't exist
        const formFields = ['name', 'email', 'subject', 'message'];
        formFields.forEach(field => {
            const input = document.getElementById(field);
            if (input && !document.getElementById(`${field}-error`)) {
                const errorDiv = document.createElement('div');
                errorDiv.className = 'error-message';
                errorDiv.id = `${field}-error`;
                input.parentNode.appendChild(errorDiv);
            }
        });
 
        // Contact form submission with validation
        contactForm.addEventListener('submit', function(e) {
            e.preventDefault();

            // Validate form
            if (!validateForm()) {
                return;
            }

            // Create loading indicator
            const submitBtn = contactForm.querySelector('button[type="submit"]');
            const originalBtnText = submitBtn.textContent;
            submitBtn.disabled = true;
            submitBtn.innerHTML = `<i class="fas fa-spinner fa-spin"></i> ${translations[currentLang].sending}`;

            // Prikupi sva polja forme (uključujući honeypot polje)
            const formData = new FormData(contactForm);

            // Send data using fetch API
            fetch('php/process-contact.php', {
                method: 'POST',
                body: formData
            })
            .then(response => response.json()
                .then(data => ({ ok: response.ok, data }))
                .catch(() => ({ ok: response.ok, data: {} }))
            )
            .then(({ ok, data }) => {
                if (!ok || data.status !== 'success') {
                    throw new Error(data.message || 'Server error');
                }

                // Show success message
                const successMessage = document.createElement('div');
                successMessage.className = 'success-message';
                successMessage.textContent = translations[currentLang].thankYou;

                contactForm.innerHTML = '';
                contactForm.appendChild(successMessage);
            })
            .catch(error => {
                // Handle errors
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalBtnText;

                // Show error message
                const errorMessage = document.createElement('div');
                errorMessage.className = 'error-message form-error';
                errorMessage.textContent = 'There was a problem sending your message. Please try again later.';

                try {
                    // Add to form container
                    submitBtn.parentNode.appendChild(errorMessage);

                    // Remove error message after 5 seconds
                    setTimeout(() => {
                        if (errorMessage.parentNode) {
                            errorMessage.parentNode.removeChild(errorMessage);
                        }
                    }, 5000);
                } catch (e) {}
            });
        });
    }
 
    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function(e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                window.scrollTo({
                    top: target.offsetTop - 80,
                    behavior: 'smooth'
                });
            }
        });
    });
 
    // Create back to top button
    const backToTopBtn = document.createElement('div');
    backToTopBtn.className = 'back-to-top';
    backToTopBtn.innerHTML = '<i class="fas fa-chevron-up"></i>';
    backToTopBtn.setAttribute('title', translations[currentLang].backToTop);
    document.body.appendChild(backToTopBtn);
 
    // Show/hide back to top button
    window.addEventListener('scroll', function() {
        if (window.scrollY > 500) {
            backToTopBtn.classList.add('active');
        } else {
            backToTopBtn.classList.remove('active');
        }
    });
 
    // Scroll to top when clicked
    backToTopBtn.addEventListener('click', function() {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });
 
    // Animation on scroll (simple version)
    function animateOnScroll() {
        const animatedElements = document.querySelectorAll('.service-card, .project-card, .stat-item, .about-content, .testimonial-slider, .contact-content');
 
        animatedElements.forEach(element => {
            const elementPosition = element.getBoundingClientRect().top;
            const screenPosition = window.innerHeight;
 
            if (elementPosition < screenPosition - 100) {
                element.style.opacity = '1';
                element.style.transform = 'translateY(0)';
            }
        });
    }
 
    // Set initial styles for animated elements
    document.querySelectorAll('.service-card, .project-card, .stat-item, .about-content, .testimonial-slider, .contact-content').forEach(element => {
        element.style.opacity = '0';
        element.style.transform = 'translateY(30px)';
        element.style.transition = 'all 0.8s ease';
    });
 
    // Initialize AOS animation library
    if (typeof AOS !== 'undefined') {
        AOS.init({
            duration: 800,
            easing: 'ease-in-out',
            once: true,
            mirror: false
        });
    }
 
    // Custom cursor
    const cursorInner = document.querySelector('.cursor-inner');
    const cursorOuter = document.querySelector('.cursor-outer');
 
    if (cursorInner && cursorOuter) {
        document.addEventListener('mousemove', function(e) {
            cursorInner.style.left = e.clientX + 'px';
            cursorInner.style.top = e.clientY + 'px';
 
            cursorOuter.style.left = e.clientX + 'px';
            cursorOuter.style.top = e.clientY + 'px';
        });
 
        document.addEventListener('mousedown', function() {
            cursorInner.style.transform = 'translate(-50%, -50%) scale(0.7)';
        });
 
        document.addEventListener('mouseup', function() {
            cursorInner.style.transform = 'translate(-50%, -50%) scale(1)';
        });
 
        // Add cursor-visible class after a short delay
        setTimeout(() => {
            document.body.classList.add('cursor-visible');
        }, 1000);
 
        // Add hover effect to interactive elements
        const interactiveElements = document.querySelectorAll('a, button, .btn, .project-card, .service-card');
 
        interactiveElements.forEach(el => {
            el.addEventListener('mouseenter', () => {
                document.body.classList.add('cursor-hover');
            });
 
            el.addEventListener('mouseleave', () => {
                document.body.classList.remove('cursor-hover');
            });
        });
 
        // Hide cursor when mouse leaves the window
        document.addEventListener('mouseleave', function() {
            document.body.classList.remove('cursor-visible');
        });
 
        document.addEventListener('mouseenter', function() {
            document.body.classList.add('cursor-visible');
        });
    }
 
    // Dark mode toggle
    const themeToggle = document.querySelector('.theme-toggle');
 
    if (themeToggle) {
        const themeIcon = themeToggle.querySelector('i');
 
        // Check for saved theme preference
        const savedTheme = localStorage.getItem('theme');
        if (savedTheme === 'dark') {
            document.body.classList.add('dark-mode');
            themeIcon.classList.remove('fa-moon');
            themeIcon.classList.add('fa-sun');
        }
 
        themeToggle.addEventListener('click', function() {
            document.body.classList.toggle('dark-mode');
 
            if (document.body.classList.contains('dark-mode')) {
                themeIcon.classList.remove('fa-moon');
                themeIcon.classList.add('fa-sun');
                localStorage.setItem('theme', 'dark');
            } else {
                themeIcon.classList.remove('fa-sun');
                themeIcon.classList.add('fa-moon');
                localStorage.setItem('theme', 'light');
            }
        });
    }
 
    // Run animations once on page load
    animateOnScroll();
    window.addEventListener('scroll', animateOnScroll);
 
    // Language selector implementation
    const langButtons = document.querySelectorAll('.lang-btn');
 
    langButtons.forEach(btn => {
        btn.addEventListener('click', function() {
            const lang = this.getAttribute('data-lang');
            const langSelector = this.closest('.lang-selector');
 
            // Update active state on buttons
            langButtons.forEach(b => b.classList.remove('active'));
            this.classList.add('active');
 
            // Update data-lang attribute on selector
            langSelector.setAttribute('data-lang', lang);
 
            // Add/remove sr-active class for the slider animation
            if (lang === 'sr') {
                langSelector.classList.add('sr-active');
            } else {
                langSelector.classList.remove('sr-active');
            }
 
            // Call updateLanguage function
            updateLanguage(lang);
        });
    });
 
    // Check if there's a saved language preference
    const savedLang = localStorage.getItem('preferredLanguage');
    if (savedLang) {
        // Get the language selector
        const langSelector = document.querySelector('.lang-selector');
 
        // Set the proper active class on the language button
        langButtons.forEach(btn => {
            if (btn.getAttribute('data-lang') === savedLang) {
                btn.classList.add('active');
            } else {
                btn.classList.remove('active');
            }
        });
 
        // Set the data-lang attribute on the selector
        langSelector.setAttribute('data-lang', savedLang);
 
        // Add/remove sr-active class based on saved language
        if (savedLang === 'sr') {
            langSelector.classList.add('sr-active');
        } else {
            langSelector.classList.remove('sr-active');
        }
 
        // Update the page content to the saved language
        updateLanguage(savedLang);
    } else {
        // If no saved language, set default language (en)
        updateLanguage('en');
    }
 
    // Implement FAQ accordion functionality
    const faqItems = document.querySelectorAll('.faq-item');
 
    faqItems.forEach(item => {
        const question = item.querySelector('.faq-question');
        
        question.addEventListener('click', () => {
            // Check if this item is already active
            const isActive = item.classList.contains('active');
            
            // Close all items
            faqItems.forEach(faqItem => {
                faqItem.classList.remove('active');
            });
            
            // If the clicked item wasn't active, make it active
            if (!isActive) {
                item.classList.add('active');
            }
        });
    });
 
    // Add updateFAQLanguage function to update language
    function updateFAQLanguage(lang) {
        if (!document.querySelector('#faq')) return;
        
        // Update FAQ section header
        document.querySelector('#faq .section-header h2').innerHTML = translations[lang].faqTitle;
        document.querySelector('#faq .section-header p').textContent = translations[lang].faqDesc;
        
        // Update FAQ questions and answers
        const faqItems = document.querySelectorAll('.faq-item');
        if (faqItems.length === 0) return;
        
        // First FAQ item
        if (faqItems[0]) {
            faqItems[0].querySelector('h3').textContent = translations[lang].faqQuestion1;
            faqItems[0].querySelector('.faq-answer p').textContent = translations[lang].faqAnswer1;
        }
        
        // Second FAQ item
        if (faqItems[1]) {
            faqItems[1].querySelector('h3').textContent = translations[lang].faqQuestion2;
            faqItems[1].querySelector('.faq-answer p').textContent = translations[lang].faqAnswer2;
        }
        
        // Third FAQ item (payment methods)
        if (faqItems[2]) {
            faqItems[2].querySelector('h3').textContent = translations[lang].faqQuestion3;
            faqItems[2].querySelector('.faq-answer p').textContent = translations[lang].faqAnswer3;
        }
        
        // Fourth FAQ item (technologies)
        if (faqItems[3]) {
            faqItems[3].querySelector('h3').textContent = translations[lang].faqQuestion4;
            faqItems[3].querySelector('.faq-answer p').textContent = translations[lang].faqAnswer4;
            
            const techCategories = faqItems[3].querySelectorAll('.tech-category h4');
            if (techCategories.length >= 4) {
                techCategories[0].textContent = translations[lang].frontend;
                techCategories[1].textContent = translations[lang].backend;
                techCategories[2].textContent = translations[lang].mobile;
                techCategories[3].textContent = translations[lang].databases;
            }
        }
        
        // Fifth FAQ item
        if (faqItems[4]) {
            faqItems[4].querySelector('h3').textContent = translations[lang].faqQuestion5;
            faqItems[4].querySelector('.faq-answer p').textContent = translations[lang].faqAnswer5;
        }
        
        // Sixth FAQ item
        if (faqItems[5]) {
            faqItems[5].querySelector('h3').textContent = translations[lang].faqQuestion6;
            faqItems[5].querySelector('.faq-answer p').textContent = translations[lang].faqAnswer6;
        }
        
        // Seventh FAQ item (collaboration process)
        if (faqItems[6]) {
            faqItems[6].querySelector('h3').textContent = translations[lang].faqQuestion7;
            faqItems[6].querySelector('.faq-answer p').textContent = translations[lang].faqAnswer7;
    
            const processSteps = faqItems[6].querySelectorAll('.faq-answer ol li');
            if (processSteps.length > 0) {
                processSteps[0].textContent = translations[lang].initialTalk;
                processSteps[1].textContent = translations[lang].planning;
                processSteps[2].textContent = translations[lang].design;
                processSteps[3].textContent = translations[lang].development;
                processSteps[4].textContent = translations[lang].testing;
                processSteps[5].textContent = translations[lang].launch;
                processSteps[6].textContent = translations[lang].support;
            }
        }
    }
 
    // Modify the existing updateLanguage function to include FAQ section
    const originalUpdateLanguage = window.updateLanguage;
    if (originalUpdateLanguage) {
        window.updateLanguage = function(lang) {
            // Call the original function first
            originalUpdateLanguage(lang);
            
            // Then update the FAQ section
            updateFAQLanguage(lang);
        };
        
        // Update FAQ with current language
        updateFAQLanguage(currentLang);
    }
 
    // Add FAQ link to navigation menu
    const contactLink = document.querySelector('.nav-menu ul li:nth-child(5)');
 
    if (navMenu && contactLink) {
        const faqLink = document.createElement('li');
        const faqAnchor = document.createElement('a');
        faqAnchor.setAttribute('href', '#faq');
        faqAnchor.textContent = currentLang === 'en' ? 'FAQ' : 'FAQ';
        faqLink.appendChild(faqAnchor);
        
        navMenu.insertBefore(faqLink, contactLink);
        
        // Update the nav links indexes for future language updates
        translations.en.faq = "FAQ";
        translations.sr.faq = "FAQ";
        
        // Update the original language update function to handle the new menu item
        if (originalUpdateLanguage) {
            window.updateLanguage = function(lang) {
                // Call the modified function
                originalUpdateLanguage(lang);
                
                // Update the FAQ link
                const faqNavLink = document.querySelector('.nav-menu li:nth-child(5) a');
                if (faqNavLink) {
                    faqNavLink.textContent = translations[lang].faq;
                }
                
                // Then update the FAQ section
                updateFAQLanguage(lang);
            };
            
            // Update with current language
            updateLanguage(currentLang);
        }
    }
 
    // Animate team members on scroll
    const teamMembers = document.querySelectorAll('.team-member');
 
    function animateTeamOnScroll() {
        teamMembers.forEach(member => {
            const memberPosition = member.getBoundingClientRect().top;
            const screenPosition = window.innerHeight;
 
            if (memberPosition < screenPosition - 100) {
                member.style.opacity = '1';
                member.style.transform = 'translateY(0)';
            }
        });
    }
    const newsletterForm = document.getElementById('newsletterForm');
    
    if (newsletterForm) {
        newsletterForm.addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Get the visible email input based on current language
            const currentLang = document.documentElement.getAttribute('lang') || 'en';
            const isSerbian = currentLang === 'sr';
            
            // Get the appropriate email input
            const emailInput = isSerbian 
                ? document.getElementById('newsletter-email')
                : document.getElementById('newsletter-email-en');
            
            // Get or create message container
            let messageContainer = document.querySelector('.newsletter-message');
            if (!messageContainer) {
                messageContainer = document.createElement('div');
                messageContainer.className = 'newsletter-message';
                newsletterForm.appendChild(messageContainer);
            }
            
            // Validate email
            if (!emailInput || emailInput.value.trim() === '') {
                showNewsletterMessage(
                    messageContainer, 
                    isSerbian ? 'Email adresa je obavezna' : 'Email is required', 
                    'error'
                );
                return;
            }
            
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(emailInput.value)) {
                showNewsletterMessage(
                    messageContainer, 
                    isSerbian ? 'Unesite validnu email adresu' : 'Please enter a valid email', 
                    'error'
                );
                return;
            }
            
            // Show loading state
            const submitBtn = isSerbian 
                ? newsletterForm.querySelector('.sr-content[type="submit"]')
                : newsletterForm.querySelector('.en-content[type="submit"]');
                
            const originalBtnText = submitBtn.textContent;
            submitBtn.disabled = true;
            
            // Different loading text based on language
            if (isSerbian) {
                submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Šalje se...';
            } else {
                submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Sending...';
            }
            
            // Create form data
            const formData = new FormData();
            formData.append('newsletter_email', emailInput.value);
            
            // Send data using fetch API
            fetch('php/process-newsletter.php', {
                method: 'POST',
                body: formData
            })
            .then(response => {
                if (!response.ok) {
                    throw new Error('Network response was not ok');
                }
                return response.text();
            })
            .then(data => {
                // Handle success
                emailInput.value = '';
                
                showNewsletterMessage(
                    messageContainer, 
                    isSerbian 
                        ? 'Hvala što ste se prijavili na naš newsletter!' 
                        : 'Thank you for subscribing to our newsletter!', 
                    'success'
                );
                
                // Reset button
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalBtnText;
            })
            .catch(error => {
                // Handle error
                showNewsletterMessage(
                    messageContainer, 
                    isSerbian 
                        ? 'Došlo je do problema sa vašom prijavom. Molimo pokušajte ponovo.' 
                        : 'There was a problem with your subscription. Please try again.', 
                    'error'
                );
                
                // Reset button
                submitBtn.disabled = false;
                submitBtn.innerHTML = originalBtnText;
            });
        });
    }
    
    // Helper function to display messages
    function showNewsletterMessage(container, message, type) {
        // Clear any existing messages
        container.innerHTML = '';
        container.className = 'newsletter-message ' + type;
        
        // Add new message
        container.textContent = message;
        
        // Add sr-content or en-content class based on current language
        const currentLang = document.documentElement.getAttribute('lang') || 'en';
        if (currentLang === 'sr') {
            container.classList.add('sr-content');
            container.classList.remove('en-content');
        } else {
            container.classList.add('en-content');
            container.classList.remove('sr-content');
        }
        
        // Auto-clear success messages after 5 seconds
        if (type === 'success') {
            setTimeout(() => {
                container.innerHTML = '';
                container.className = 'newsletter-message';
            }, 5000);
        }
    }
    // Dodajte ovu funkciju u vaš main.js za poboljšane animacije projekata

// Funkcija za animaciju projekata pri učitavanju
function animateProjectsOnLoad() {
    const projectsSection = document.getElementById('projects');
    if (!projectsSection) return;
    
    // Animiraj naslov i opis
    const header = projectsSection.querySelector('.section-header');
    if (header) {
        header.style.opacity = '0';
        header.style.transform = 'translateY(20px)';
        header.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
        
        setTimeout(() => {
            header.style.opacity = '1';
            header.style.transform = 'translateY(0)';
        }, 100);
    }
    
    // Animiraj filtere
    const filters = projectsSection.querySelectorAll('.filter-btn');
    filters.forEach((filter, index) => {
        filter.style.opacity = '0';
        filter.style.transform = 'translateY(20px)';
        filter.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
        
        setTimeout(() => {
            filter.style.opacity = '1';
            filter.style.transform = 'translateY(0)';
        }, 300 + (index * 100));
    });
    
    // Animiraj kartice projekata
    const projects = projectsSection.querySelectorAll('.project-card');
    projects.forEach((project, index) => {
        project.style.opacity = '0';
        project.style.transform = 'translateY(30px)';
        project.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
        
        setTimeout(() => {
            project.style.opacity = '1';
            project.style.transform = 'translateY(0)';
        }, 600 + (index * 150));
    });
}

// Funkcija za kružne animacije na hover dugmadima za filtere
function setupFilterButtonHoverEffects() {
    const filterBtns = document.querySelectorAll('.filter-btn');
    
    filterBtns.forEach(btn => {
        btn.addEventListener('mouseenter', function(e) {
            if (!this.querySelector('.filter-btn-bg')) {
                const circle = document.createElement('span');
                circle.classList.add('filter-btn-bg');
                this.appendChild(circle);
                
                // Postavite položaj kruga tačno gde je miš
                const rect = this.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                circle.style.left = x + 'px';
                circle.style.top = y + 'px';
                
                // Pokrenite animaciju
                setTimeout(() => {
                    circle.classList.add('animate');
                }, 10);
                
                // Uklonite krug nakon animacije
                setTimeout(() => {
                    if (circle.parentNode === this) {
                        this.removeChild(circle);
                    }
                }, 500);
            }
        });
    });
}

// CSS stilovi koje treba dodati za animaciju kruga
function addFilterButtonHoverStyles() {
    // Proverite da li stil već postoji
    if (!document.getElementById('filter-btn-styles')) {
        const style = document.createElement('style');
        style.id = 'filter-btn-styles';
        style.innerHTML = `
            .filter-btn {
                position: relative;
                overflow: hidden;
            }
            
            .filter-btn-bg {
                position: absolute;
                background-color: rgba(108, 92, 231, 0.2);
                border-radius: 50%;
                transform: scale(0);
                width: 120px;
                height: 120px;
                pointer-events: none;
                z-index: -1;
            }
            
            .filter-btn-bg.animate {
                animation: filterBtnRipple 0.5s ease-out forwards;
            }
            
            @keyframes filterBtnRipple {
                0% {
                    transform: scale(0);
                    opacity: 1;
                }
                100% {
                    transform: scale(1);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);
    }
}
    // Add language change listener to update visibility
    document.addEventListener('languageChanged', function(e) {
        const lang = e.detail.language;
        
        // Update message container visibility if it exists
        const messageContainer = document.querySelector('.newsletter-message');
        if (messageContainer) {
            if (lang === 'sr') {
                messageContainer.classList.add('sr-content');
                messageContainer.classList.remove('en-content');
            } else {
                messageContainer.classList.add('en-content');
                messageContainer.classList.remove('sr-content');
            }
        }
    });
    // Set initial styles for team members
    teamMembers.forEach(member => {
        member.style.opacity = '0';
        member.style.transform = 'translateY(30px)';
        member.style.transition = 'all 0.8s ease';
    });
 
    // Run animation on scroll
    window.addEventListener('scroll', animateTeamOnScroll);
    
    // Run animation once on page load
    animateTeamOnScroll();
 
    // Initialize cookie consent
    CookieConsent.init();

     // Dodajte CSS stilove za hover efekte
     addFilterButtonHoverStyles();
    
     // Postavite hover efekte na dugmad za filtere
     setupFilterButtonHoverEffects();
     
     // Animirajte projekte pri učitavanju
     animateProjectsOnLoad();
 });

